# Netbox image version (leave blank for default: v4.5-4.0.0)
VERSION=

# Root directory for all Netbox bind mounts
# (configuration, media, reports, scripts, postgres, redis, redis-cache)
NETBOX_DATA_ROOT=/srv/netbox

# See also: env/netbox.env, env/postgres.env, env/redis.env, env/redis-cache.env
