# Syncthing config — persistent settings, keys, certs
SYNCTHING_CONFIG_PATH=/path/to/docker-configs/syncthing

# Sync data directory — where Syncthing stores received/synced files
SYNCTHING_DATA_PATH=/path/to/docker-data/syncthing
