# 1. Service
apiVersion: v1
kind: Service
metadata:
  name: dozzle-service
  namespace: external-infra
spec:
  ports:
    - name: http
      protocol: TCP
      port: 80          # Traefik talks to 80
      targetPort: 8383
---
# 2. EndpointSlice (The Bridge)
apiVersion: discovery.k8s.io/v1
kind: EndpointSlice
metadata:
  name: dozzle-slice
  namespace: external-infra
  labels:
    kubernetes.io/service-name: dozzle-service
addressType: IPv4
ports:
  - name: http
    port: 8383
    protocol: TCP
endpoints:
  - addresses:
      - "${DOZZLE_IP}"
    conditions:
      ready: true
---
# 3. Ingress (The Route)
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dozzle-ingress
  namespace: external-infra
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
    cert-manager.io/cluster-issuer: "letsencrypt-prod-cf"
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - "dozzle.${BASE_DOMAIN}"
      secretName: dozzle-tls-secret
  rules:
    - host: "dozzle.${BASE_DOMAIN}"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: dozzle-service
                port:
                  number: 80
