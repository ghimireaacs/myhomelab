# 1. Service
apiVersion: v1
kind: Service
metadata:
  name: shuttle-service
  namespace: external-infra
spec:
  ports:
    - name: http
      protocol: TCP
      port: 80          # Traefik talks to 80
      targetPort: 5000
---
# 2. EndpointSlice (The Bridge)
apiVersion: discovery.k8s.io/v1
kind: EndpointSlice
metadata:
  name: shuttle-slice
  namespace: external-infra
  labels:
    kubernetes.io/service-name: shuttle-service
addressType: IPv4
ports:
  - name: http
    port: 5000
    protocol: TCP
endpoints:
  - addresses:
      - "${SHUTTLE_IP}"
    conditions:
      ready: true
---
# 3. Ingress (The Route)
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: shuttle-ingress
  namespace: external-infra
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
    cert-manager.io/cluster-issuer: "letsencrypt-prod-cf"
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - "shuttle.${BASE_DOMAIN}"
      secretName: shuttle-tls-secret
  rules:
    - host: "shuttle.${BASE_DOMAIN}"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: shuttle-service
                port:
                  number: 80
