# =============================================================================
# myK8S Environment Variables
# Copy this file to .env and fill in your values.
# Usage: set -a; source .env; set +a; envsubst < file.yaml.tmpl | kubectl apply -f -
# Or use: myK8S/scripts/apply.sh <file.yaml.tmpl>
# =============================================================================

# ---------------------------------------------------------------------------
# Base Domain
# ---------------------------------------------------------------------------
BASE_DOMAIN=example.com

# ---------------------------------------------------------------------------
# NFS Storage
# ---------------------------------------------------------------------------
NFS_SERVER_IP=

# Media service config paths
NFS_DOWNLOADS_PATH=/mnt/k8s-data/downloads
NFS_SONARR_CONFIG_PATH=/mnt/k8s-data/media/sonarr
NFS_RADARR_CONFIG_PATH=/mnt/k8s-data/media/radarr
NFS_PROWLARR_CONFIG_PATH=/mnt/k8s-data/media/prowlarr
NFS_READARR_CONFIG_PATH=/mnt/k8s-data/media/readarr
NFS_AUDIOBOOKSHELF_CONFIG_PATH=/mnt/k8s-data/media/audiobookshelf
NFS_KAVITA_CONFIG_PATH=/mnt/k8s-data/media/kavita
NFS_LAZYLIBRARIAN_CONFIG_PATH=/mnt/k8s-data/media/lazylibrarian
NFS_RDTCLIENT_CONFIG_PATH=/mnt/k8s-data/media/rdtclient

# Personal service paths
NFS_DOCUMENTS_PATH=/mnt/k8s-data/documents
NFS_PAPERLESS_CONFIG_PATH=/mnt/k8s-data/paperless
NFS_SEARXNG_PATH=/mnt/k8s-data/searxng
NFS_GLANCE_PATH=/mnt/k8s-data/glance
NFS_WALLOS_PATH=/mnt/k8s-data/wallos
NFS_KARAKEEP_WEB_PATH=/mnt/k8s-data/karakeep/web
NFS_KARAKEEP_MEILI_PATH=/mnt/k8s-data/karakeep/meilisearch
NFS_TRILIUM_PATH=/mnt/k8s-data/trilium
NFS_VIKUNJA_PATH=/mnt/k8s-data/vikunja
NFS_WALLABAG_PATH=/mnt/k8s-data/wallabag
NFS_WALLABAG_POSTGRES_PATH=/mnt/k8s-data/wallabag/postgres

# Infra paths
NFS_LOKI_PATH=/mnt/k8s-data/loki
NFS_GRAFANA_PATH=/mnt/k8s-data/grafana
NFS_SYSLOG_NG_PATH=/mnt/k8s-data/syslog-ng
NFS_NETBOX_POSTGRES_PATH=/mnt/k8s-data/netbox/postgres
NFS_NETBOX_MEDIA_PATH=/mnt/k8s-data/netbox/media

# ---------------------------------------------------------------------------
# MetalLB IP Pool
# ---------------------------------------------------------------------------
METALLB_IP_POOL_START=
METALLB_IP_POOL_END=

# ---------------------------------------------------------------------------
# cert-manager / Let's Encrypt
# ---------------------------------------------------------------------------
CERT_MANAGER_EMAIL=
CLOUDFLARE_API_TOKEN=

# ---------------------------------------------------------------------------
# Prometheus scrape targets (YAML list format for envsubst)
# e.g. ["192.168.1.10:9100", "192.168.1.11:9100"]
# ---------------------------------------------------------------------------
NODE_EXPORTER_TARGETS=["192.168.x.x:9100","192.168.x.x:9100"]
CADVISOR_TARGETS=["192.168.x.x:8080","192.168.x.x:8080"]
# Note: exclude LXC containers from cadvisor targets — no Docker daemon
K8S_NODE_EXPORTER_TARGETS=["192.168.x.x:9100","192.168.x.x:9100"]

# ---------------------------------------------------------------------------
# Authentik (Helm values)
# ---------------------------------------------------------------------------
AUTHENTIK_SECRET_KEY=
AUTHENTIK_POSTGRES_PASSWORD=

# ---------------------------------------------------------------------------
# Grafana
# ---------------------------------------------------------------------------
GRAFANA_ADMIN_PASSWORD=
GRAFANA_OAUTH_CLIENT_ID=
GRAFANA_OAUTH_CLIENT_SECRET=
AUTHENTIK_BASE_URL=https://authentik.example.com
GRAFANA_ROOT_URL=https://grafana.example.com

# ---------------------------------------------------------------------------
# ntfy (Grafana alerting)
# ---------------------------------------------------------------------------
NTFY_ADMIN_TOKEN=

# ---------------------------------------------------------------------------
# Paperless-ngx
# ---------------------------------------------------------------------------
PAPERLESS_POSTGRES_PASSWORD=
PAPERLESS_SECRET_KEY=
PAPERLESS_ADMIN_USER=admin
PAPERLESS_ADMIN_PASSWORD=

# ---------------------------------------------------------------------------
# Karakeep
# ---------------------------------------------------------------------------
KARAKEEP_APP_SECRET_KEY=
KARAKEEP_NEXTAUTH_SECRET=
KARAKEEP_SMTP_PASSWORD=
OLLAMA_BASE_URL=https://ai.example.com

# ---------------------------------------------------------------------------
# Glance
# ---------------------------------------------------------------------------
GLANCE_SECRET_TOKEN=

# ---------------------------------------------------------------------------
# Vikunja
# ---------------------------------------------------------------------------
VIKUNJA_JWT_SECRET=

# ---------------------------------------------------------------------------
# Wallabag
# ---------------------------------------------------------------------------
WALLABAG_POSTGRES_PASSWORD=

# ---------------------------------------------------------------------------
# SearXNG
# ---------------------------------------------------------------------------
SEARXNG_SECRET_KEY=

# ---------------------------------------------------------------------------
# NetBox (K8s)
# ---------------------------------------------------------------------------
NETBOX_DB_PASSWORD=
NETBOX_SECRET_KEY=
NETBOX_API_TOKEN_PEPPER_1=

# ---------------------------------------------------------------------------
# NetBox2 (K8s)
# ---------------------------------------------------------------------------
NETBOX2_DB_PASSWORD=
NETBOX2_SECRET_KEY=
NETBOX2_REDIS_PASSWORD=
NETBOX2_REDIS_CACHE_PASSWORD=

# ---------------------------------------------------------------------------
# External Service IPs
# ---------------------------------------------------------------------------
PROXMOX_IP=
SHUTTLE_IP=
HOMEASSISTANT_IP=
NEXTCLOUD_IP=
N8N_IP=
PORTAINER_IP=
PIHOLE_IP=
OLLAMA_IP=
COPYPARTY_IP=
RADICALE_IP=
TRUENAS_IP=
IMMICH_IP=
UPTIME_KUMA_IP=
DOZZLE_IP=
YT_DL_IP=
TEST_SERVICE_IP=
JELLYFIN_IP=
NTFY_IP=

# ---------------------------------------------------------------------------
# Semaphore (Ansible UI)
# ---------------------------------------------------------------------------
SEMAPHORE_DB_PASSWORD=
SEMAPHORE_ADMIN_PASSWORD=
SEMAPHORE_ADMIN_EMAIL=
# ACCESS_KEY_ENCRYPTION: openssl rand -base64 32 (any length ok)
SEMAPHORE_ACCESS_KEY_ENCRYPTION=
# COOKIE_HASH and COOKIE_ENCRYPTION must be exactly 32 chars: openssl rand -hex 16
SEMAPHORE_COOKIE_HASH=
SEMAPHORE_COOKIE_ENCRYPTION=
SEMAPHORE_TOTP_SECRET=

# Forgejo
NFS_FORGEJO_PATH=/mnt/ghostdata/ghostsilo/99_System/K8S-Data/forgejo

# Forgejo Runner
NFS_FORGEJO_RUNNER_PATH=/mnt/ghostdata/ghostsilo/99_System/K8S-Data/forgejo-runner
FORGEJO_RUNNER_TOKEN=your-runner-token-from-forgejo-admin
TRAEFIK_LB_IP=your-traefik-lb-ip
