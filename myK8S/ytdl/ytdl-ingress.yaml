apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ytdl-ingress
  namespace: personal
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
    cert-manager.io/cluster-issuer: "letsencrypt-prod-cf"
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - "yt.${BASE_DOMAIN}"
      secretName: ytdl-tls-secret
  rules:
    - host: "yt.${BASE_DOMAIN}"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ytdl-svc
                port:
                  number: 8080
