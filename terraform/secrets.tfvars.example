# Proxmox provider credentials
# Copy to secrets.tfvars and fill in real values — never commit secrets.tfvars

pm_api_url          = "https://<proxmox-host>:8006/api2/json"
pm_api_token_id     = "terraform@pam!<token-name>"
pm_api_token_secret = "<uuid-token-secret>"
pm_tls_insecure     = true
